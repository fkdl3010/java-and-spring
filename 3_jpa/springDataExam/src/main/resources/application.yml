

spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

  datasource:
    username: sa
    password: 1234
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/springDataJpaExam
server:
  port: 8080
